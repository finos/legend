---
ownership, classification, documentation, lineage hints, etc. and to drive downstream tooling.
(`{profile.tag = 'value'}`) coming from a `Profile`. This is useful to capture metadata such as
can be annotated with **stereotypes** (`<<profile.stereotype>>`) and **tagged values**
Like any other Pure element, a `Database` (and its inner `Schema`, `Table`, `Column` and `View`)

---
description: Example of attaching profile stereotypes and tagged values to a Database, Schema, Table, Column and View.
title: Relational Database Specification with Stereotypes and Tagged Values

